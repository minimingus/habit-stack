name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  migrate:
    name: Apply DB Migrations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Apply migrations
        run: supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}

  build:
    name: Build & Test
    runs-on: macos-15
    needs: migrate
    steps:
      - uses: actions/checkout@v4

      - name: Cache Homebrew
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/Homebrew
            /usr/local/Cellar/mint
          key: brew-mint-${{ runner.os }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: brew-mint-${{ runner.os }}-

      - name: Install Mint
        run: brew install mint

      - name: Cache Mint packages
        uses: actions/cache@v4
        with:
          path: ~/.mint
          key: mint-${{ runner.os }}-${{ hashFiles('Mintfile', '.github/workflows/ci.yml') }}
          restore-keys: mint-${{ runner.os }}-

      - name: Install XcodeGen
        run: mint install yonaskolb/XcodeGen

      - name: Write Secrets.plist
        run: |
          cat > HabitStack/Secrets.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>SupabaseURL</key><string>${{ secrets.SUPABASE_URL }}</string>
            <key>SupabaseAnonKey</key><string>${{ secrets.SUPABASE_ANON_KEY }}</string>
            <key>RevenueCatAPIKey</key><string>${{ secrets.REVENUECAT_API_KEY }}</string>
            <key>PostHogAPIKey</key><string>${{ secrets.POSTHOG_API_KEY }}</string>
          </dict>
          </plist>
          PLIST

      - name: Generate Xcode project
        run: mint run xcodegen generate

      - name: Cache SPM packages
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('**/Package.resolved', 'project.yml') }}
          restore-keys: spm-${{ runner.os }}-

      - name: Resolve SPM packages
        run: xcodebuild -resolvePackageDependencies -project HabitStack.xcodeproj -scheme HabitStack

      - name: Build
        run: |
          xcodebuild build-for-testing \
            -project HabitStack.xcodeproj \
            -scheme HabitStack \
            -destination "platform=iOS Simulator,OS=latest,name=iPhone 16" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty

      - name: Test
        run: |
          xcodebuild test-without-building \
            -project HabitStack.xcodeproj \
            -scheme HabitStackTests \
            -destination "platform=iOS Simulator,OS=latest,name=iPhone 16" \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty
